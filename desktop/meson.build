project('reactNativeGstPlayerDemo', 'c')

# Dependencies
shared_dependencies = [
    dependency('gstreamer'),
    dependency('json-glib-1.0')
]

includes_dir = include_directories([
    '../native'
])

subdir('src')
